schema: https://backend.ljh305.shop/graphql # you can also point to a GraphQL endpoint!
generates:
  ./src/commons/types/generated/types.ts:
    plugins:
      - typescript
    config:
      typesPrefix: I
