export default function Button(props : any){
    return (
        <select>
            {/* {
                props.optionList.map((el:string) => {
                    return <option key={uuid}>{el}</option>
                })
            } */}
        </select>
    )
}